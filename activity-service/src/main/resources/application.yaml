server:
  port: 8082
spring:
  application:
    name: activity-service
  datasource:
    url: jdbc:mysql://localhost:3306/fitmedia-activity-db
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
